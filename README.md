Install:
> 1. bundle install
> 2. rake db:migrate
> 3. rake db:seed

Run:
> foreman start

Adminpanel login
> user: 'admin@example.com', pass: 'password'
> user: 'staff@example.com', pass: 'password'
